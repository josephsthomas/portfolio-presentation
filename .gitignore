# Extracted image assets (465 MB, too large for git — lives in GitHub release)
extracted/assets/
site/assets/

# Source PowerPoint is hosted as a GitHub release asset, not in git
*.pptx

# Editor / OS
.DS_Store
*.swp
.vscode/
.idea/
